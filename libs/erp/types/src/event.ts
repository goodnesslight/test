export enum EventType {
  PRACTICE = 'practice',
  MATCH = 'match',
}

export enum EventAttendanceStatus {
  GOING = 'going',
  MAYBE = 'maybe',
  DECLINED = 'declined',
}

export enum EventScope {
  SINGLE = 'single',
  SERIES = 'series',
}
