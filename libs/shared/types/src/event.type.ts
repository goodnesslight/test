export enum EventType {
  PRACTICE = 'practice',
  SCRIM = 'scrim',
  MATCH = 'match',
  TOURNAMENT = 'tournament',
}

export enum AttendanceStatus {
  GOING = 'going',
  MAYBE = 'maybe',
  DECLINED = 'declined',
}
